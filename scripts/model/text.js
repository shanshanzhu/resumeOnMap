define(["backbone","jquery","handlebar"], function(){

  var textmodel = Backbone.Model.extend({
    // defaults: {
    //   "options": 
    // },

    initialize: function() {
      
    },

  });

  return textmodel;
});